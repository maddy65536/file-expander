#include <stdio.h>

int expandFile(char *input, char *output) {
    printf("expanding:\ninput: %s\noutput: %s\n", input, output);
    return 0;
}

int unexpandFile(char *input, char *output) {
    printf("unexpanding:\ninput: %s\noutput: %s\n", input, output);
    return 0;
}
